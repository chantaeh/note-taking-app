import { Outlet } from "react-router-dom";

function Layout() {
    return (
        <>
            <h1>Lotion</h1>
            <p>Like Notion, but worse.</p>
        </>
    );
}

export default Layout;